using UnityEngine;

public class PrintToConsole : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("We are starting the journey, we better jump this...");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
